# 长任务进度

更新时间：2026-08-15

## 当前稳定基线

- 稳定提交：`d3893a1` 之后的 Tier 62（另有 `fb448cd` 纯公司章三重参考核验）。
- 6 张及扩展样单的既有全量基线：表单字段 3010/3010、商品明细 2817/2817、商品行 313/313。
- 日期：301 张中识别 221 张；机器判定为可靠的 196/196 正确。
- 印章：严格匹配 228/301；机器判定为可靠的 227/227 正确。
- 既有自动化测试：419 项通过。

## Tier 63 日期低置信度审计（进行中）

已完成：

- 审计了“日期值正确但置信度不足”的样本，并选取 `7272556241.jpg` 为目标样本。
- 复查 `7302125543.jpg` 的右侧日数字：Mobile OCR 能读到 `5`，Server OCR 仍不完整，证据不足，保持待人工复核。
- 发现低置信度人工候选的多个派生图会被错误当作独立证据，导致 `7272556241.jpg` 从 0.68 虚高到 0.997。
- 调整日期审计逻辑：可靠性只由原始决策级证据计算；同一图像的审计派生结果不再累积成自动可靠结论。
- 新增针对上述防累积逻辑的单元测试；相关 2 项定向测试通过，`py_compile` 通过。

尚未完成：

- 安全修复后的目标样本与危险负例真实 OCR 重跑。
- 301 张样本全量指标重算。
- 全套测试回归、文档更新和 Tier 63 正式验收。

下次从安全修复后的目标/负例重跑开始；在完成全量回归前，不把 Tier 63 视为准确率提升成果。

相关审计结果位于：

- `storage/far-lower-date-day-probe-tier63.json`
- `storage/e2e-date-current-rerun-7272556241-tier63.json`
- `storage/e2e-date-component-audit-7272556241-tier63.json`
